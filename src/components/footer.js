import React from 'react';

export default class extends React.Component {
    render() {
        return (
            <div>
                <p>{this.props.foot}</p>
            </div>
        );
    }
}